---
kanban-plugin: board
---

> [!warning] MIGRATED to the SimplerDevelopment portal
> This board now lives in the portal as project **"Visual Editor QA"** (id 150): **https://simplerdevelopment.com/portal/projects/150**. Track status there. This markdown file is a **frozen snapshot as of 2026-07-02** (82 cards migrated: Backlog 34 · Validating 32 · Approved 16) — do not update it; move cards in the portal.

## Backlog

- [ ] `VEQA-001` **[meta]** Captured from 8 solo visual-editor review recordings on 2026-07-01 (13:11, 13:46, 13:58, 14:08, 14:12, 14:19, 14:22, 15:02 EDT). `[📹 video-confirmed]` = screenshot evidence pulled; `[⚠ no screen share]` = 13:46 recording had no shared screen. Frame refs are `video@timestamp`.
- [ ] `VEQA-002` **[dashboard]** Widgetize the entire dashboard — plug-and-play widgets; fold Quick Actions into the widget system `[13:11 00:00:49]`
- [ ] `VEQA-003` **[dashboard]** Move AI Credits off the dashboard → bottom of left sidebar under Settings, above System Mode `[13:11 00:01:55]`
- [ ] `VEQA-004` **[dashboard]** Remove the "your services" block from the dashboard (handle in Settings) `[13:11 00:01:55]`
- [ ] `VEQA-005` **[dashboard]** Website dashboard: componentize/widgetize (drag-drop shortcuts) same as main dashboard `[13:11 00:05:42]`
- [ ] `VEQA-006` **[portal]** All-sites listing (`portal/websites`): show a homepage thumbnail per site `[13:11 00:03:41]`
- [ ] `VEQA-007` **[portal]** Entries listing tables: add SEO columns — has title / description / preview image / OG + Twitter tags (checkboxes) `[13:11 00:06:34]`
- [ ] `VEQA-008` **[portal]** Fix publish→saving button width jump (button grows too wide when toggling to "saving") `[13:11 00:06:34]`
- [ ] `VEQA-009` **[dev-platform]** Spike: developer API keys + per-website developer access (builder.io model) — `[decision: aligned]` `[13:11 00:04:58]`
- [ ] `VEQA-010` **[dev-platform]** Link/create the developer SDK GitHub repo under the simpler-development org `[13:11 00:05:42]`
- [ ] `VEQA-011` **[editor-core]** Drag-drop: allow dropping a new block as a CHILD of an existing element (today it must already be a parent) — same bug on sections & columns `[13:11 00:09:09] [13:46 00:01:02]` — **Dx:** [PROJECT] New-block-from-palette drop always splices to root (`EditableBlockRenderer` handleDrop ~L286-320) — never calls `insertIntoContainer`; needs container hit-testing on drop. Med risk.
- [ ] `VEQA-013` **[editor-core]** Heading typography broken: font-family missing (Google key rotated?), controls don't apply `[13:11 00:14:14]`
- [ ] `VEQA-014` **[editor-core]** Text/heading color: can't apply color to multiple selected items (or one); highlight color broken `[13:11 00:13:17]`
- [ ] `VEQA-015` **[editor-core]** Autosave: editor loses all blocks on refresh — restore last state `[13:11 00:25:05]` — **Dx:** [PROJECT · HIGH] `usePostForm.ts:273` disables REST autosave when `ydoc` merely *exists* (not connected); if realtime never connects, edits never persist and refresh reverts. Needs a connected-vs-degraded fallback design decision (touches collab persistence across 2 processes).
- [ ] `VEQA-018` **[editor-core]** 🅴 ESCALATED — Drag-to-resize height (section / bg-image) doesn't sync to the panel value — must type it manually `[📹 v1311@27:40]` ![[v1311_2740_dragheight.jpg]] — **RC (style-vs-responsiveStyle seam, resize-path variant):** the resize handle (`SelectableBlock.tsx` → `VisualEditorShell.onBlockResized`) writes flat `block.style.height` (what the live element renders from), but the panel's Height field reads/writes `block.responsiveStyle[currentViewport].height` and its `effectiveStyle = {...style, ...responsive}` spreads responsive last — so the moment a user types a height once, the panel pins to the responsive slot and every later drag (which only updates `style.height`) becomes invisible to the panel. **Why escalated:** a clean fix needs a decision on which slot is canonical for dimensions (flat `style` vs per-breakpoint `responsiveStyle`) and touches the editor shell + resize/postMessage path + panel read + renderer sizeStyle — beyond a mechanical change, and the SAME style-model decision that gates #3's tablet/mobile follow-up and #6's deferred Max Width/Font Family. **Recommended fix (pending decision):** on resize, `onBlockResized` writes width/height into BOTH `style` (keeps the renderer) AND `responsiveStyle[currentViewport]` (keeps the panel in sync); requires plumbing `currentViewport` into the shell handler.
- [ ] `VEQA-020` **[section]** Section has TWO places to set a background image — remove the duplicate `[13:11 00:25:05]` — **Dx:** [DONE — duplicate of VEQA-075] Section Content tab bg-image already removed (commits 8dfdbbb3 / d89b851a); Style→Background is now the only place. Close as dup. (Note: the separate deck/popup editor still has a dup Max Width — its own follow-up.)
- [ ] `VEQA-023` **[section]** Box shadow needs more controls: color, per-side positioning, spread `[13:11 00:30:25]` — **Dx:** [PROJECT · small] Box Shadow is one canned `<select>`; build a `BoxShadowBuilder` (offset/blur/spread/color/inset) mirroring `GradientBuilder`, compose the string. Must round-trip-parse legacy presets.
- [ ] `VEQA-024` **[section]** Clarify/remove "transition" & "blend mode" — document what they do `[13:11 00:28:31]`
- [ ] `VEQA-032` **[styling-global]** Typography inheritance: parent (section/column) typography incl. COLOR + font-size must cascade to children until the child overrides `[13:46 00:07:03] [14:08 00:00:39] [15:02]` — **Dx:** [PROJECT · HIGH] Leaf renderers hardcode `text-foreground` + explicit size classes (12+ files, prod + editor trees), defeating CSS inheritance; plus a data-model ambiguity (size 'unset' vs explicit 'base'). Needs its own spec (CSS-native vs context cascade) before any edit.
- [ ] `VEQA-034` **[styling-global]** `[decision: adversarial review]` Combine static padding/margin into a single padding/margin control (vs separate) `[13:46 00:03:48]`
- [ ] `VEQA-035` **[styling-global]** `[decision: adversarial review]` Is the Layout tab still needed after width/height are split out? (images specifically) `[14:12 00:01:15]`
- [ ] `VEQA-038` **[button]** Add an "overridden" indicator when a Style-tab value overrides a Content-tab value `[13:58 00:00:39]` — **Dx:** [PROJECT · small] Add an 'overridden' badge in `ContentPanel` when `block.style` overrides a Content value (button already computes hasCustomBg/Color/FontSize); wants a shared `<OverrideBadge>`.
- [ ] `VEQA-039` **[button]** Variant select on the Content tab doesn't work — fix (size works) `[13:58 00:00:00]` — **Dx:** [QUICK + DECISION] `secondary` variant emits `bg-secondary` etc. but `--secondary` is undefined in `@theme` (Tailwind v4 → classes compile to nothing); `size` uses universal utilities so it works. Fix = add `--secondary`/`--color-secondary` tokens OR repoint secondary to `--accent`/`--muted` (design-system call). Med risk (global token).
- [ ] `VEQA-041` **[button]** Show hover-effect preview in the editor while editing `[13:58 00:01:34]` — **Dx:** [PROJECT · small] Hover-effect preview: pointer-events already reach the Link, but the mouse is over the panel while choosing — needs an editor-only force-hover state + `.force-hover` CSS mirror threaded from `ContentPanel`.
- [ ] `VEQA-042` **[button]** Clarify what the "brand preset" optional button/input does — document `[13:58 00:01:34]`
- [ ] `VEQA-051` **[gallery]** Expand gallery to 6 columns each (full 12-grid) `[14:22 00:01:45]` — **Dx:** [PROJECT] Widen gallery columns type (2|3|4→up to 12) + switch to inline `gridTemplateColumns` (Tailwind can't purge dynamic `grid-cols-N`).
- [ ] `VEQA-058` **[spacers]** Investigate vertical/horizontal spacer; verify editor height ↔ style-panel height correlation `[14:22]` — **Dx:** [QUICK opt-b] `SpacerBlockRender` ignores the Style-tab Height (reads only the Content enum); either hide that dead field for spacers (quick) or merge responsiveStyle (ties to the escalated VEQA-018 decision).
- [ ] `VEQA-059` **[sticky-scroll]** Record a separate demo video for the sticky-scroll tabs component `[14:22]` — **Dx:** [USER ACTION — not code] Record a demo video of the sticky-scroll component.
- [ ] `VEQA-061` **[marquee]** Font + color pickers for text; color for icons `[15:02 00:00:20]` — **Dx:** [DONE-ish] Marquee text font+color / icon color already work via Style tab → Text/Icon Items sub-tabs. Per-item override would be a project. Confirm intent (discoverability vs per-item).
- [ ] `VEQA-063` **[marquee]** Horizontal text flow when direction is up/down `[15:02 00:01:24]` — **Dx:** [PROJECT] Horizontal text on up/down is a `react-fast-marquee` limitation (rotates 90°); needs a custom vertical render path.
- [ ] `VEQA-064` **[cta]** Rich-text description with proper height on the Content tab `[15:02]` — **Dx:** [likely DONE — re-verify] CTA description is already multi-line `RichTextEditable` w/ `min-h-[60px]`; the 'no height' may be the toolbar clipping near the panel top. Re-check live.
- [ ] `VEQA-065` **[cta]** Remove background from the Content tab (Style tab only) `[15:02]` — **Dx:** [PROJECT · small] Same class as VEQA-075 — remove the Content-tab Background Style select; make Style-tab bg authoritative while preserving the default branded gradient (renderer + type + defaults).
- [ ] `VEQA-067` **[cta]** Evaluate merging Hero + CTA tabs → rename to "hero cta" if redundant `[15:02]` — **Dx:** [DECISION] Merge Hero+CTA is a product call (high-risk migration if merged); low-risk alt = align inconsistent element-key names. Needs sign-off.
- [ ] `VEQA-068` **[card-grid]** Per-card icon color control `[15:02]` — **Dx:** [PROJECT] Per-card icon color blocked on the missing card-grid per-card editor UI (`DynamicPanel` has no per-card controls); also add `iconColor` to the card type.
- [ ] `VEQA-070` **[card-grid]** Fix: card background color not applying `[15:02]` — **Dx:** [REPRO NEEDED] Static analysis found no card-grid bg defect (card-grid IS wrapped in `BlockStyleWrapper`, unlike columns) — needs a live repro to identify which background (per-card field doesn't exist; uniform + Style-tab both trace clean).


## Planned



## In Progress



## Validating

- [ ] `VEQA-056` **[accordion]** Content → rich text; add a variant that supports child blocks `[14:22]` — **[FIXED · worker batch-2] accordion content→rich text via new ListEditor 'richtext' type (child-blocks variant = [PROJECT]). Gate: tsc/eslint 0-err. Commit `bba22b1a`. QA pending.**
- [ ] `VEQA-036` **[button]** Default max-width 250px (currently full width) + keep "Click Me" text `[📹 v1358@00:00]` — **[FIXED] default `style.maxWidth:'250px'` + `ButtonBlockRender` now reads `style.maxWidth` on the <Link>. **Verified live:** button capped ~250px. Gate: eslint/tsc 0-err. Commit `e04fbe3c`.**
- [ ] `VEQA-037` **[button]** Default alignment: center `[13:58 00:00:39]` — **[FIXED] button default `alignment: 'center'` (legacy runtime fallback left as 'left'). **Verified live.** Commit `e04fbe3c`.**
- [ ] `VEQA-040` **[button]** Material-icon name field → dropdown with actual icon previews `[13:58 00:00:39]` — **[FIXED] Content-tab Icon field is now the searchable `IconPicker` (glyph previews) instead of a plain text field. **Verified live:** '★ Star' picker renders. Commit `e04fbe3c`.**
- [ ] `VEQA-043` **[button]** Remove the Layout feature from buttons (redundant with the Content tab) `[13:58 00:01:58]` — **[FIXED] extended `noDisplayControls` to `button` so the Style-tab Display/flex/grid Layout controls are hidden (Size/Max-Width stay). Commit `e04fbe3c`.**
- [ ] `VEQA-044` **[button]** Text-decoration (underline/strike) color must match the font color `[13:58 00:04:23]` — **[FIXED] `ButtonBlockRender` now applies `style.textDecoration` on the <Link> with `textDecorationColor: currentColor`, so the underline/strike matches the font color. **Verified live:** white text → white underline. Commit `e04fbe3c`.**
- [ ] `VEQA-027` **[columns]** Sibling columns resize relatively when one width changes (no invalid 50% + 75%) `[⚠ no screen share] [13:46 00:01:02]` — **RC:** the right-panel width slider set only the dragged column's width (no rebalance) → invalid totals (e.g. 50%+75%). **Fix:** `updateColumnWidth` now takes the delta from the other columns proportionally (10% floor) and normalizes to sum 100. Gate: eslint 0-err, tsc 0-err. **Verify in Validating:** drag a slider, others rebalance to 100%. Commit `823a2b36`.
- [ ] `VEQA-028` **[columns]** Column width/separator drag updates the right panel but not the live canvas — sync `[⚠ no screen share] [13:46 00:02:45]` — **RC:** the column-separator drag (`ColumnsEditorOverlay`) posts new widths to the parent but never optimistically applies them inside the iframe; `VisualEditorShell.onColumnResized`/`onGapChanged` nonetheless set `iframeOriginatedRef=true` (echo-suppression), so the sync effect skipped pushing the update back down — the panel updated (reads parent state) but the canvas didn't (reads iframe-local state). **Fix:** don't set `iframeOriginatedRef` for column-resize/gap, so the update syncs back to the iframe canvas. Gate: eslint 0-err, tsc 0-err. **Not screenshot-verified** (needs a pixel-precise iframe separator drag) — please confirm the drag in Validating. Commit `0e329da6`.
- [ ] `VEQA-029` **[columns]** Mute layout features that don't apply to columns (e.g. display:flex) in the column Style tab `[⚠ no screen share] [13:46 00:02:45]` — **RC:** `StyleSettings` rendered Display/flex/grid Layout controls for every block incl. columns, which manages its own layout. **Fix:** added `noDisplayControls = block.type==='columns'` gate (mirrors the `nonTextBlocks` pattern) around Display/Direction/Justify/Align/Wrap/Gap/Grid; Align-Self/Size/Overflow stay. **Verified live:** columns Style→Layout no longer shows flex/grid controls. Gate: eslint 0-err, tsc 0-err. Commit `823a2b36`.
- [ ] `VEQA-030` **[columns]** Gap controls overlap the height controls — reposition (can't set column height without the right panel) `[⚠ no screen share — could not screenshot] [13:46 00:04:18]` — **RC:** the gap-drag pill (`ColumnsEditorOverlay`) sat at bottom-center with z-index 55, over the section height resize handle (bottom-center, z-51), stealing its clicks. **Fix:** offset the pill to `left: calc(50% + 70px)` so it clears the height handle. **Verified live:** the 'gap: md' pill is now right of center. Gate: eslint 0-err, tsc 0-err. Commit `823a2b36`.
- [ ] `VEQA-031` **[columns]** Background setting not working on columns `[⚠ no screen share] [13:46 00:04:18]` — **RC:** `ColumnsBlockRender` (production) never wrapped its root in `BlockStyleWrapper`/read `block.style`, so a Style-tab background worked in the editor canvas (which *does* wrap it via `EditableBlockRenderer`) but was dropped on published/preview pages. (Separately, the per-column `column.backgroundColor` the renderer reads has no editor UI that writes it — noted as a follow-up gap.) **Fix:** wrap `ColumnsBlockRender`'s root in `BlockStyleWrapper block={block}` (same wrapper the editor path uses). **Verified:** editor canvas shows the columns-block green bg (rounded, 60/40). Production-render live-check blocked by the page-scoped preview-token gate — fix is the identical wrapper to the verified editor path. Gate: eslint 0-err, tsc 0-err. Commit `0e329da6`.
- [ ] `VEQA-066` **[cta]** Tighten padding/alignment (currently "goofy / too tight") `[15:02 00:02:16]` — **[FIXED · worker batch] CTA baseline vertical padding (guarded so authored padding wins). Gate: tsc/eslint 0-err. Commit `905413b4`. QA pending.**
- [ ] `VEQA-055` **[divider]** Add a color selector in the Style tab `[14:22]` — **RC:** `DividerBlockRender` checked `style.borderColor` to drop the default class but never applied the color (it lands on the wrapper, and border-color doesn't inherit to the `<hr>`). **Fix:** apply `style={{ borderColor }}` inline on the `<hr>`. Gate: eslint 0-err, tsc 0-err. Commit `545220cd`.
- [ ] `VEQA-012` **[editor-core]** Duplicate is broken everywhere: right-click (canvas + layers panel) and ⌘D don't duplicate (images work via keyboard only) `[13:11 00:09:37]` — **RC (two bugs):** (1) right-click Duplicate (canvas + layers) routed to `useBulkActions.bulkDuplicate`, which found the block recursively but computed the insert index with a **flat** `updated.findIndex()` — so it silently no-op'd for any block nested in columns/tabs/section. (2) ⌘D was swallowed by `SelectableBlock`'s `EditableContent` keydown handler, which `stopPropagation()`'d **every** key while text was focused, so ⌘D never reached the editor's window handler (images worked because they have no editable text node to attach that listener). **Fix:** (1) `bulkDuplicate` now uses the recursive `deepCloneBlock` + `insertBlockAfter` helpers; (2) the editable keydown handler stops propagation for all keys **except** Cmd/Ctrl+D (preserving the Backspace/Delete guard). **Verified live:** right-click Duplicate on a text block nested inside a column now inserts a second copy inside that column (was a no-op). ⌘D-while-editing fix is code-reasoned, not separately screenshot-verified — please confirm the keyboard path in Validating. Gate: eslint 0-err, tsc 0-err. Commit `4af497ba`.
- [ ] `VEQA-016` **[editor-core]** Add visibility toggles (desktop/tablet/mobile) on ALL editor blocks (right panel) `[13:11 00:19:49] [13:46 00:03:48]` — **[FIXED · worker batch-2] explicit desktop/tablet/mobile visibility checkboxes on all blocks. Gate: tsc/eslint 0-err. Commit `9f49b37c`. QA pending.**
- [ ] `VEQA-017` **[editor-core]** Right-hand column: some controls overflow off the page — wrap to two rows / keep on-page `[13:11 00:19:49]` — **[FIXED · worker batch] right-panel overflow: TokenColorPicker min-w-0 + per-side border row wraps. Gate: tsc/eslint 0-err. Commit `2920b25b`. QA pending.**
- [ ] `VEQA-019` **[editor-core]** React loop on style inputs — values won't change fluidly (gradient drag & blend-mode also stuck) `[13:11 00:28:31]` — **[FIXED · worker batch-2] gradient stops given stable ids + echo-guard so the dragged stop no longer sticks. Gate: tsc/eslint 0-err. Commit `16c1ecf0`. QA pending.**
- [ ] `VEQA-071` **[flip-card]** Default overline / title / rich-text description fields; support 12 columns `[15:02]` — **[FIXED · worker batch] flip-card seeds default overline/title/description. Gate: tsc/eslint 0-err. Commit `905413b4`. QA pending.**
- [ ] `VEQA-072` **[flip-card]** Move accent color from the Content tab → per-card settings `[15:02]` — **[FIXED · worker batch] flip-card accent → per-card (block-level kept as default fallback). Gate: tsc/eslint 0-err. Commit `2920b25b`. QA pending.**
- [ ] `VEQA-073` **[flip-card]** Audit backgrounds + typography consistency across all card / flip-card Style tabs `[15:02]` — **[FIXED · worker batch-2] flip-card frontImage + team-flip-grid header element-style keys wired. Gate: tsc/eslint 0-err. Commit `e49cfe15`. QA pending.**
- [ ] `VEQA-050` **[gallery]** 2–3 default images in gallery content; enable duplicate-image `[14:22 00:01:00]` — **[FIXED · worker batch] gallery 2-3 default images + duplicate-image button. Gate: tsc/eslint 0-err. Commit `b68ba5d1`. QA pending.**
- [ ] `VEQA-052` **[gallery]** Style sub-tabs: block / caption / modal / images (border-radius + bg image; modal color/bg) `[14:22 00:02:52]` — **[FIXED · worker batch-2] gallery modal/images Style sub-tabs (elementStyles threaded into Lightbox). Gate: tsc/eslint 0-err. Commit `e49cfe15`. QA pending.**
- [ ] `VEQA-060` **[marquee]** Collapsible + duplicatable content segments; adjustable segment height/size `[15:02 00:00:20]` — **[FIXED · worker batch-2] marquee segments collapsible + duplicatable (per-segment size = [PROJECT]). Gate: tsc/eslint 0-err. Commit `75594b1c`. QA pending.**
- [ ] `VEQA-062` **[marquee]** Icon field → material-icon dropdown with autocomplete search `[15:02 00:00:20]` — **[FIXED · worker batch] marquee icon field → IconPicker dropdown. Gate: tsc/eslint 0-err. Commit `905413b4`. QA pending.**
- [ ] `VEQA-074` **[metric-card]** Rename "institution logo" label → footer text; allow logo/image; drop the institution association `[15:02]` — **[FIXED · worker batch] metric-card 'institution logo'→footer-text wording + MediaPicker (field names preserved). Gate: tsc/eslint 0-err. Commit `2920b25b`. QA pending.**
- [ ] `VEQA-021` **[section]** Border width + border radius need custom-entry fields `[13:11 00:28:31]` — **[FIXED · worker batch] border width/radius custom-entry fields. Gate: tsc/eslint 0-err. Commit `2920b25b`. QA pending.**
- [ ] `VEQA-022` **[section]** Section bg with a border overflows past the border even with overflow set — fix `[13:11 00:29:38]` — **RC:** `overflow` from block.style was applied to the `BlockStyleWrapper` ancestor (a plain rectangle; sections are `selfStyled` so it has no border-radius), while bg+border+radius live on the section `<Tag>` — so clipping happened on the wrong element and the bg bled past the rounded border even with overflow set. The `<Tag>` only got `overflow-hidden` when `splitColor` was set. **Fix:** apply `overflow` on the section `<Tag>` itself (`SectionBlockRender` + `EditableBlockRenderer`) — honor explicit `s.overflow`, default `hidden` when a border-radius is present so the bg clips to the rounded corners. **Verified live** (bg image + 8px border + 32px radius): bg now stays inside the rounded border. Gate: eslint 0-err. Commit `4a6e310d`.
- [ ] `VEQA-025` **[section]** Clean up style categories — image blocks shouldn't show a Typography category `[13:11 00:22:19]` — **RC:** `StyleSettings` gated only the font *fields* with `hasTextContent` but rendered the Typography section header + Text Color/Font Family/etc. unconditionally, so image blocks still showed a Typography category. **Fix:** wrap the whole Typography `StyleSection` in `{hasTextContent && ...}`. Gate: eslint 0-err, tsc 0-err. Commit `545220cd`.
- [ ] `VEQA-033` **[styling-global]** Standardize one height/width section across ALL blocks' Style tabs (pulled out of the Layout tab) `[13:58 00:01:58]` — **[FIXED · worker batch-2] Size fields extracted into own Style section (+ Min Width/Max Height), standardized across blocks. Gate: tsc/eslint 0-err. Commit `1349c4dc`. QA pending.**
- [ ] `VEQA-057` **[tabs]** Style sub-tabs: block / tabs / tab bar / content area `[14:22]` — **RC:** `TabsBlockRender` already calls `getElementCSS(elementStyles,'tab'/'activeTab'/'tabPanel')` but `block-elements.ts` had no `tabs` entry, so the Style sub-tabs were never exposed. **Fix:** add a `tabs` entry (Block / Tab Bar / Active Tab / Content Area) — zero render change. Gate: eslint 0-err, tsc 0-err. Commit `545220cd`.
- [ ] `VEQA-048` **[video]** YouTube block: drop full-video handling; caption → rich text on Content tab; style sub-tabs block/video/caption `[14:19 — no recording]` — **[FIXED · worker batch-2] youtube caption→rich text + caption sub-tab (full-video handling left as [PROJECT]). Gate: tsc/eslint 0-err. Commit `6b237a0a`. QA pending.**
- [ ] `VEQA-049` **[video]** Video block: caption → rich text; style sub-tabs (block/video/caption); show filename not full path `[14:22 00:00:29]` — **[FIXED · worker batch] video caption→rich text + style sub-tabs + picker shows filename not full path. Gate: tsc/eslint 0-err. Commit `b68ba5d1`. QA pending.**

## Approved

- [x] `VEQA-026` **[columns]** ✅ QA-PASS (rubric, 2026-07-02) — always-visible per-column delete (trash) button; **functional** (Columns count 2→1 on clicking "delete column 2"). Gates: G1 intent ✅, G2 editor ✅ (prod N/A — editor-only control, doesn't change render), G3 ✅ (properties-panel control, viewport-independent), G4 N/A (editor chrome), G5 ✅ recording+screens, G6 ✅ scoped. Evidence: `veqa-026/02-columns-selected.png`, `03-after-delete-col2.png` + video. Commit `823a2b36`. **Awaiting human ship.**
- [x] `VEQA-054` **[code]** ✅ QA-PASS (rubric, 2026-07-02) — code Style tab shows sub-tabs **Block / Title Bar / Body**. Evidence: `veqa-batch/VEQA-054/panel.png`. Commit `905413b4`. **Awaiting human ship.**
- [x] `VEQA-046` **[quote]** ✅ QA-PASS (rubric, 2026-07-02) — quote Style tab shows sub-tabs **Block / Quote / Author / Citation** (Citation present). Evidence: `veqa-batch/VEQA-046/panel.png`. Commit `905413b4`. **Awaiting human ship.**
- [x] `VEQA-045` **[quote]** ✅ QA-PASS (rubric, 2026-07-02) — quote Content tab: content/author/citation are all **contenteditable (rich text)**. Evidence: `veqa-batch2/VEQA-045/panel.png`. Commit `b68ba5d1`. **Awaiting human ship.**
- [x] `VEQA-047` **[image]** ✅ QA-PASS (rubric, 2026-07-02) — image Content tab shows Alt/Caption/**Alignment**, **no Width** (Width moved to Style). Evidence: `veqa-batch2/VEQA-047/panel.png`. Commit `b68ba5d1`. **Awaiting human ship.**
- [x] `VEQA-053` **[code]** ✅ QA-PASS (rubric, 2026-07-02) — code Content tab: Language field is a **`<select>` dropdown** (js/ts/…). Evidence: `veqa-batch2/VEQA-053/panel.png`. Commit `b3be0943`. **Awaiting human ship.**
- [x] `VEQA-069` **[card-grid]** ✅ QA-PASS (rubric, 2026-07-02) — card-grid Content tab: Columns select goes **2…12**. (Per-card rich-text description remains a [PROJECT], separate.) Evidence: `veqa-batch2/VEQA-069/panel.png`. Commit `bf90bca4`. **Awaiting human ship.**
- [ ] `VEQA-075` **[section]** ✅ approved — Move Background image + Background color + Max Width OFF the Content tab → Style tab `[📹 v1311@26:35]` ![[v1311_2635_contenttab_bg.jpg]] — **RC:** the section Content tab (`LayoutPanel`) duplicated Style-tab fields, but wrote *legacy top-level props* (`block.backgroundColor`/`.maxWidth`/`.fontFamily`) while the Style tab writes `block.style.*` / `block.responsiveStyle.*`; the renderer honored the legacy locations for bg/text-color but ignored the Style-tab locations for Max Width (`responsiveStyle.maxWidth`) and Font Family (`style.fontFamily`). **v1 (`8dfdbbb3`):** removed the 3 safe fields. **v2 completion (`d89b851a`):** made `SectionBlockRender` + `EditableBlockRenderer` honor `responsiveStyle.desktop.maxWidth` (on the inner container) and `style.fontFamily` (inline), then removed Max Width + Font Family from the Content tab too. **Verified live:** Content tab now shows only Anchor ID / HTML Tag / Diagonal Split; a Style-tab Max Width of 320px caps + centers the section content and a Style-tab Font Family renders (Georgia serif). HTML Tag stays on Content (structural). Gate: eslint 0-err, tsc 0-err.
- [ ] `VEQA-076` **[dashboard]** ✅ approved — Fix per-widget padding — many widgets hug the left edge `[📹 v1311@02:30]` ![[v1311_0230_dashpad.jpg]] — **RC:** `WidgetShell` header has `px-3` but its **body wrapper had no horizontal padding**, so every widget's content hugged the card edge unless the widget self-inset; only the Metric widgets did (via `MetricBlock`'s own `px-[15px]`). **v2 fix (reworked after review — first attempt only patched 2 widgets):** added `px-3 pb-3` to the shared `WidgetShell` body so **all** widgets inset uniformly, and removed `MetricBlock`'s own `px-[15px]` (it has no bg/border) so the metric tiles don't double-pad. Reverted the earlier 2-widget patch. **v3 (review: still missing TOP padding in many cases):** changed body `px-3 pb-3` → `p-3` so content no longer hugs the header border either. **Verified live with ALL 26 widgets enabled** — metric (Active Projects), grid (CRM Snapshot), list+badge (Hosting Status), action-list (AI Connect, Agency & White-Label), summary+list (Invoices), Store/Email: every widget now has consistent inset on all four sides. Gate: eslint clean, tsc 0-err. Commits `61f035bd` → `ddadc1b5` → `eefaac76`.
- [ ] `VEQA-077` **[editor-core]** Media pickers inconsistent: background "Select Media" shows the full library but the image-upload picker shows a subset — unify the source `[📹 v1311@21:30]` ![[v1311_2130_rhcol_offpage_b.jpg]] — **RC:** Style-tab bg `MediaPicker` passes no `apiEndpoint` → defaults to client-wide `/api/portal/media` (full library); Content-tab `MediaPanel` picker passed site-scoped `/api/portal/cms/websites/{siteId}/media` (subset). **Fix:** `MediaPanel` now uses the client-wide `/api/portal/media` too (both client-scoped/tenancy-safe — verified live: image picker went from 3 site-241 items to the full ~19-image library, parity with the bg picker). ⚠️ Note: the site-scoped route was designed on purpose; this unifies toward *full library* per the review cards — flag if per-site was actually wanted. Gate: eslint clean. Commit `d1079e77`.
- [ ] `VEQA-078` **[image]** "Select media" must access the whole library (parity with the bg-image picker) `[14:12 00:00:03]` — **Closed by the media-picker fix above (same commit `d1079e77`).** Verified live.
- [ ] `VEQA-079` **[dashboard]** Company-switcher dropdown renders transparent over sidebar text — add solid bg / z-index `[📹 v1311@02:54]` ![[v1311_0254_companyswitcher.jpg]] — **RC:** not transparency; `globals.css:566 .portal-chrome-dark > *{z-index:1}` made header (switcher) & `<nav>` equal-z siblings, nav painted over the dropdown (visual bleed **and** click interception — the SimplerDevelopment option literally couldn't be clicked). **Fix:** added `.portal-chrome-dark > *:first-child{z-index:40}` (higher specificity, same stylesheet) to lift the header's stacking context. **Verified live:** dropdown now renders fully solid + option clicks work. Gate: CSS-only (no lint/tsc scope). Commit `5fb7f632`.
- [ ] `VEQA-080` **[button]** Border width / radius / shadow apply to BOTH container and button — apply to the button only; button shouldn't have a fat container `[📹 v1358@05:05]` ![[v1358_0505_borderdup.jpg]] — **Same root cause + same fix as the button-bg card above. Verified live.** Commit `6f432d67`.
- [ ] `VEQA-081` **[image]** Default/fallback placeholder image on all image blocks + a height on load `[📹 v1412@00:03 — image block renders empty]` ![[v1412_0003_selectmedia.jpg]] — **RC:** `ImageBlockRender.tsx:42 if(!block.url) return null` → block collapsed to 0-height/invisible/unselectable. **Fix:** render a placeholder box (aspect-ratio 16/9, min-height 160, image icon, inline colors for theme independence) when no url. **Verified live** in editor on post 1967: empty image block now shows a visible placeholder with height. Gate: eslint clean (0 errors). Commit `92d24277`.
- [ ] `VEQA-082` **[button]** Button bg color paints the whole section instead of just the button (branding conflict) `[📹 v1358@03:42]` ![[v1358_0342_btnbg.jpg]] — **RC (shared with border card):** `button` isn't in `BLOCK_ELEMENTS`, so its Style tab writes `block.style`, which `BlockStyleWrapper` paints on the full-width wrapper band; `ButtonBlockRender` also copies a subset onto the narrow `<Link>` → bg colors the whole band + border doubles. **Fix:** exclude `button` from wrapper bg/border/shadow (like `section`) and apply the full set — incl. the previously-dropped `boxShadow`/`opacity` — on the `<Link>` only. **Verified live** (post 1967, button given bg/border/shadow): bg, border, radius, shadow now render on the button only; no band, no container border. Gate: eslint clean. Commit `6f432d67`.
- [ ] `VEQA-083` **[editor-core]** Flex row broken: items in a `direction:row` section stack vertically instead of side-by-side `[📹 v1311@17:56]` ![[v1311_1756_flexrow.jpg]] — **RC (style-vs-responsiveStyle seam):** the Style-tab Display/Direction controls write `block.responsiveStyle.desktop`, but `SectionBlockRender`/`EditableBlockRenderer` computed the children container's layout from flat `block.style` only, so `display:flex`/`flex-direction:row` never reached the inner container → children stayed `display:block`. **Fix:** both renderers now merge `responsiveStyle.desktop` over `block.style` for the inner container's layout props (display/flexDirection/justify/align/wrap/gap/grid). **Verified live:** Item A/B/C now sit side-by-side. (Desktop path fixed; tablet/mobile per-breakpoint layout is a further step on the same seam.) Gate: eslint 0 errors. Commit `e957803c`.


## Shipped





%% kanban:settings
```
{"kanban-plugin":"board","list-collapse":[false,false,false,false,false,false]}
```
%%