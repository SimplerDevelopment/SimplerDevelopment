# A/B Engine — Overnight 2026-05-07

Tracking entry for the A/B testing engine work on `feat/ab-engine-polymorphic`.

## Log

- Docs (docs/ab-testing-guide): AB_TESTING_GUIDE.md, 10 sections, 2646 words.
- W3.D (feat/ab-survey-variants-e2e): Branched off `origin/feat/ab-engine-polymorphic`. New Playwright spec `tests/e2e/survey-variants-lifecycle.spec.ts` mirrors W3.B conventions — `setup/fixtures.ts` `clientApi`, serial-mode describe, `runCleanups` in `afterAll`, `@ab @critical` tag. Drives W1.C's full surface: POST `/api/portal/surveys` (default draft) → POST `/variants` ×2 (A weight 50, B weight 50) → GET list (id ASC, A then B) → PATCH B `weight=80` → PATCH B `enabled=false` → PUT survey `status='active'` → public GET `/api/surveys/:slug` with fresh `sd_visitor` cookie asserts `variantId===A.id`, `variantName==='A'`, `fields[0].id==='name'` (deterministic since B is the only other option and now disabled) → public POST response with `variantId=A.id` (201) → tamper-guard arms (negative id → 400; valid-but-foreign id 999_999_999 → 201 silently nulled, documenting the W1.C tradeoff) → GET `/variants/stats` asserts ≥1 row under variant A → portal UI smoke (login → /portal/surveys/:id, both A and B visible) → DELETE variant A (B remains, A GET 404s) → DELETE survey (cascade verified: survey GET 404, variants list 404, stats 404). test count: 13. typecheck: clean (0 new error types — every error in the new file is the same `Cannot find module '@playwright/test'` / `process` / `binding element implicitly any` baseline triplet that every other `tests/e2e/*.spec.ts` file emits; verified by grep against `tests/e2e/ab-experiment.spec.ts`'s baseline). files: 1 (new). Branch left local-only (uncommitted, unpushed) per overnight rules. Integration caveat: spec assumes W1.C's `feat/ab-survey-variants` (ba435ceb3) is merged or rebased into the same base — without it, `/api/portal/surveys/:id/variants*` 404s and `survey_responses.variantId` doesn't persist. Hand-off: run after W1.C lands on staging, or rebase this branch onto W1.C first.
- W2.A (feat/ab-variants-add-remove): Branched off `origin/feat/ab-engine-polymorphic`. Made A/B variants dynamic — add/remove/rename beyond the hard-coded a/b. POST `/api/portal/experiments/[id]/variants` now optional-body: when called with `{}` it auto-picks the next free letter (`c` after a+b, …, up to `z` → 409 `no_keys_available`), validates explicit keys to a SINGLE lowercase `a-z` letter, auto-fills label (`Variant <KEY>` / `Control` for `a`), inserts the row AND assigns `floor(100/(N+1))` weight to the new key, then renormalizes the experiment's `variantSplit` to sum to 100. New DELETE `/api/portal/experiments/[id]/variants/[variantKey]/route.ts` enforces three guards before dropping a row + renormalizing: `variantKey==='a'` → 400 `control_protected`; `<= 2` variants total → 409 `min_two_variants`; status `running` → 409 `experiment_running`; falls back to `evenSplit` over remaining keys when normalization would yield zero weights. PATCH on the existing route already handled `label`; left intact, added a 255-char cap. UI in `ExperimentDetailClient.tsx`: per-variant click-to-edit label (Enter saves, Esc cancels, blur saves; matches deck-title pattern from W1.D), trash-icon Remove button per row with disabled+title-text on the three guard cases, "Add variant" outline button at bottom of the variants list (capped at 26), "Rebalance to even" link in the Traffic split header that distributes `floor(100/N)` with the remainder going to the last key, and an amber `warning` row that appears whenever the draft split total ≠ 100% (`Total must equal 100% — rebalance or adjust (currently X%)`). Add/remove paths refetch via the existing GET endpoint and re-seed `draftSplit`/`variantJson` (preserves in-progress JSON edits on still-existing keys). Touched only the two route files + the detail client — `lib/ab/*`, schema, and editor untouched per task constraints. Material Icons (`add`, `delete`, `edit`, `warning`). typecheck: clean (0 new errors in touched files — only the two pre-existing baseline `drizzle-orm` cascade errors that every other `app/api/**/route.ts` emits; total error-line count unchanged at 2767 vs. baseline). files: 3 (1 new `[variantKey]/route.ts`, 2 modified). No commits, no push per overnight rules.
